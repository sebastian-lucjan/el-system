import React from 'react';
import StyledHeroImageButton from './HeroButton.styles';

const HeroImageButton = () => (
  <StyledHeroImageButton>Skontaktuj się</StyledHeroImageButton>
);

export default HeroImageButton;
