import React from 'react';
import Page from './Page';
import GlobalStyle from '../../assets/styles/globalStyles';

const Root = () => (
  <div>
    <GlobalStyle />
    <Page />
  </div>
);

export default Root;
