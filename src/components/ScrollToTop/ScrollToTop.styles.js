import styled from 'styled-components';

const StyledIcon = styled.div`
  position: fixed;
  bottom: 20px;
  right: 20px;

  .scrollToTopIcon {
    width: 30px;
    height: 30px;
    cursor: pointer;
  }
`;

export default StyledIcon;
